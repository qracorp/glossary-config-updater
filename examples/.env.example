# Environment Configuration for Glossary Configuration Updater
# Copy this file to .env and fill in your actual values

# API Configuration (Required)
API_DOMAIN=api.example.com
API_USERNAME=your_username
API_PASSWORD=your_password

# Default Configuration
DEFAULT_CONFIG_ID=config123
DEFAULT_MERGE_STRATEGY=merge

# Optional: Different environment configurations
DEV_CONFIG_ID=dev-config-456
STAGING_CONFIG_ID=staging-config-789
PROD_CONFIG_ID=prod-config-123

# Optional: API Settings
API_TIMEOUT=30
API_MAX_RETRIES=3

# Optional: Logging and Debug
LOG_LEVEL=INFO
DEBUG_MODE=false

# Optional: Notification Settings
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK
EMAIL_NOTIFICATIONS=admin@yourorg.com

# Optional: File Processing Settings
GLOSSARY_DIRECTORY=./docs/glossary
BACKUP_DIRECTORY=./backups
LOG_DIRECTORY=./logs

# Optional: Security Settings (for advanced setups)
# API_TOKEN_CACHE_TTL=3600
# API_SSL_VERIFY=true
# API_CLIENT_CERT=/path/to/client.pem
# API_CLIENT_KEY=/path/to/client.key

# Usage Examples:
# 
# 1. Basic usage with environment variables:
#    source .env
#    glossary-updater --file terms.csv --config $DEFAULT_CONFIG_ID
#
# 2. Override environment settings:
#    glossary-updater --file terms.csv --config $PROD_CONFIG_ID --merge-strategy overwrite
#
# 3. Use in scripts:
#    #!/bin/bash
#    set -a; source .env; set +a
#    glossary-updater --directory $GLOSSARY_DIRECTORY --config $DEFAULT_CONFIG_ID
